---
import copy from '../content/copy.fr.json';

const { footer } = copy;
---

<footer class="footer">
  <div class="footer__brand-banner" aria-hidden="true">
    <span class="footer__brand-display">{copy.header.logo}</span>
  </div>

  <div class="container">
    <div class="footer__grid">
      <div class="footer__brand">
        <p class="footer__logo">{copy.header.logo}</p>
        <p class="footer__description">{footer.description}</p>
        <p class="footer__tagline">{footer.tagline}</p>
        <ul class="footer__social">
          {
            footer.social.map((link) => (
              <li>
                <a href={link.href} aria-label={link.label} class="footer__social-link">
                  {
                    link.label === 'Instagram' && (
                      <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
                        <rect x="2" y="2" width="20" height="20" rx="5" ry="5" />
                        <path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z" />
                        <line x1="17.5" y1="6.5" x2="17.51" y2="6.5" />
                      </svg>
                    )
                  }
                  {
                    link.label === 'LinkedIn' && (
                      <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
                        <path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z" />
                        <rect x="2" y="9" width="4" height="12" />
                        <circle cx="4" cy="4" r="2" />
                      </svg>
                    )
                  }
                  {
                    link.label === 'Facebook' && (
                      <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
                        <path d="M18 2h-3a5 5 0 0 0-5 5v3H7v4h3v8h4v-8h3l1-4h-4V7a1 1 0 0 1 1-1h3z" />
                      </svg>
                    )
                  }
                </a>
              </li>
            ))
          }
        </ul>
      </div>

      <nav class="footer__col" aria-label="Navigation">
        <p class="footer__col-title">{footer.quickLinks.title}</p>
        <ul class="footer__col-list">
          {
            footer.quickLinks.items.map((link) => (
              <li><a href={link.href}>{link.label}</a></li>
            ))
          }
        </ul>
      </nav>

      <nav class="footer__col" aria-label="Accompagnements">
        <p class="footer__col-title">{footer.services.title}</p>
        <ul class="footer__col-list">
          {
            footer.services.items.map((link) => (
              <li><a href={link.href}>{link.label}</a></li>
            ))
          }
        </ul>
      </nav>

      <div class="footer__col">
        <p class="footer__col-title">{footer.newsletter.title}</p>
        <p class="footer__newsletter-text">{footer.newsletter.text}</p>
        <form class="footer__newsletter" action="#contact" method="get">
          <input
            type="email"
            name="email"
            placeholder={footer.newsletter.placeholder}
            aria-label={footer.newsletter.placeholder}
          />
          <button type="submit" class="btn btn--red btn--pill">{footer.newsletter.submit}</button>
        </form>
      </div>
    </div>

    <div class="footer__bottom">
      <p class="footer__copy">{footer.copyright}</p>
      <nav class="footer__links" aria-label="Liens légaux">
        {
          footer.links.map((link) => (
            <a href={link.href}>{link.label}</a>
          ))
        }
      </nav>
    </div>
  </div>
</footer>
